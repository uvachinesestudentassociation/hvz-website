# Implementation Plans

Generated by the improve skill on 2026-09-11. Execute in the order below unless dependencies say otherwise. Each executor: read the plan fully before starting, honor its STOP conditions, and update your row when done.

Do not edit application code from this index. Each plan is self-contained. Planned against commit `8edbae0`.

## Execution order & status

| Plan | Title | Priority | Effort | Depends on | Status |
|------|-------|----------|--------|------------|--------|
| 001 | Typecheck and characterization tests | P1 | M | — | DONE |
| 002 | Search deep links open the matching section | P1 | S | 001 | DONE |
| 003 | Sealed bounty fields stay off the page | P2 | S | 001 | DONE |
| 004 | Server theme lock and signed preview cookie | P1 | M | 001 | DONE |

Status values: TODO | IN PROGRESS | DONE | BLOCKED (with one-line reason) | REJECTED (with one-line rationale — finding fixed independently or approach abandoned)

## Dependency notes

- 002, 003, and 004 all require 001 because they add or update tests and there is no test script yet.
- 002 and 003 do not depend on each other. Either can follow 001.
- 004 should land after 001 so `lib/game-start.test.ts` still locks the unlock clock. It does not depend on 002 or 003.
- 004 is the highest product priority after the test runner exists. Do not start it by turning `THEME_GATE.enabled` off.

## Findings considered and rejected

Confirmed against the code after the remaining audit passes. Not planned unless asked.

- Unused shadcn kit and unused deps (`recharts`, `date-fns`, `cmdk`, `embla-carousel-react`, `react-hook-form`, `zod`, and others): not on the page import graph, so they are unlikely to ship. Not worth a plan before game week. Also unused and not shipped: `geist`, `autoprefixer` (PostCSS only loads `@tailwindcss/postcss`), and `tailwindcss-animate` (`tw-animate-css` is the one actually imported).
- Replacing anime.js hover and chevron motion with CSS: README requires anime.js for that motion. The lock screen still downloading the site is handled in 004 (do not render the shell), not by ripping the motion library out.
- Search result caps, `images.unoptimized`, and “globals.css is long”: the rules corpus is small and nothing uses `next/image`.
- `GameLiveDevPanel` as a production leak: it no-ops unless `NODE_ENV === "development"`.
- Self-hosting Compacta / dropping the cdnfonts import (`app/globals.css`): real operational risk if that CDN is down during game week, but not selected. A CSP on the preview form was also not selected; 004 keeps the lock page off theme CSS but does not add security headers.
- Game-end state from `getGameEndDate`: helper is unused and `isGameLive` stays true forever after start. README already describes a manual theme reset. Spike only if asked — do not auto-end the game in plans 001–004. Do not delete `getGameEndDate` or `GAME_DURATION_DAYS` as dead code; plan 001 characterizes them and plan 004 uses the end instant as the preview-cookie lifetime.
- Offline PWA cache: manifest exists, no service worker. A stale `content/heads-up.ts` cache is worse than no cache. Not selected.
- `pnpm lint` has no ESLint config. Left out of plan 001 so an executor does not lint-fix `components/ui/**`. Do not treat `pnpm lint` as a gate until a later plan adds a non-interactive config and confirms it exits 0 without repo-wide fixes. `AGENTS.md` was not selected.
- Theme colors are copied into CSS variables, theme TS, and hardcoded hex in components (`site-shell`, bounty, heads-up, rules accents). A one-line `ACTIVE_THEME_ID` swap will not restyle those. Not selected. Alarm-red bounty chrome looks intentional.
- Shared `parseTimeLeft` between the lock countdown and the hero countdown: same math, different clocks on purpose. Not worth a plan by itself. Do not point the lock screen at the dev live override.
- README says the countdown becomes a “Game ON” message. That string (`sections.gameOn`) is never rendered; live UI is the join badge. Local setup also omits the lock screen. README is existing editor text — do not rewrite it in these plans.
- After game start, `useClockGameLive` starts at `false` and only reads the clock in an effect, so the first paint shows the pre-game join badge even though `liveRevealed` defaults to true to prevent that flash (`hooks/use-game-live.ts`, `components/join-game-button.tsx`). Real, not selected. Fixing it can trip the T=0 ceremony if the edge detector sees a false→true transition on a hard load.
- Ceremony reduced-motion is read in an effect after `reduced` starts false, so the full timeline can start for one frame (`components/game-start-ceremony.tsx`). Not selected.
- Quest rules render twice (list item plus a hardcoded callout in `components/rules-content.tsx`). Do not delete rule copy unless the operator confirms that duplication is not intentional.
- Search shows “No rules found” on an empty query, and footnotes are not indexed. Out of scope for 002 (deep links only).
- Eastern start time uses a fixed `-4` offset. The committed 26 Oct 2026 midnight instant is correct for EDT. Do not switch to `America/New_York` in these plans; plan 001 locks the current UTC instant.
- Replacing anime.js hover and chevron motion with CSS: README requires anime.js for that motion. The lock screen still downloading the site is handled in 004 (do not render the shell), not by ripping the motion library out.
- Search result caps, `images.unoptimized`, and “globals.css is long”: the rules corpus is small and nothing uses `next/image`.
- `GameLiveDevPanel` as a production leak: it no-ops unless `NODE_ENV === "development"`.
- Self-hosting Compacta / dropping the cdnfonts import (`app/globals.css`): real operational risk if that CDN is down during game week, but not selected for this round.
- Game-end state from `getGameEndDate`: helper is unused and `isGameLive` stays true forever after start. README already describes a manual theme reset. Spike only if asked — do not auto-end the game in plans 001–004.
- Offline PWA cache: manifest exists, no service worker. A stale `content/heads-up.ts` cache is worse than no cache. Not selected.
- `pnpm lint` has no ESLint config. Left out of plan 001 so an executor does not lint-fix `components/ui/**`. Do not treat `pnpm lint` as a gate until a later plan adds a non-interactive config and confirms it exits 0 without repo-wide fixes.
